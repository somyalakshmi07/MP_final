apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ecommerce-secure.labels" . | nindent 4 }}
    app: catalog-service
spec:
  replicas: {{ .Values.services.catalog.replicas }}
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
        {{- include "ecommerce-secure.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: catalog-service
        image: "{{ .Values.image.repository }}/catalog-service:{{ .Values.image.tag }}"
        ports:
        - containerPort: {{ .Values.services.catalog.port }}
        env:
        - name: PORT
          value: "{{ .Values.services.catalog.port }}"
        - name: MONGODB_URI
          value: {{ .Values.mongodb.uri | quote }}
        {{- range $key, $value := .Values.services.catalog.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ .Values.jwtSecret.name }}
              key: {{ .Values.jwtSecret.key }}
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.catalog.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.catalog.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ecommerce-secure.labels" . | nindent 4 }}
    app: catalog-service
spec:
  selector:
    app: catalog-service
  ports:
  - port: {{ .Values.services.catalog.port }}
    targetPort: {{ .Values.services.catalog.port }}

